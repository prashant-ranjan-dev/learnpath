spring:
  application:
    name: version1
  datasource:
    url: jdbc:postgresql://localhost:5432/LearnPath
    username: postgres
    password: mysecret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  spring:
    ai:
      google:
        genai:
          model: gemini-3-flash-preview
          api-key: ${GOOGLE_API_KEY}